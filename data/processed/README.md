# Processed data

This directory contains datasets generated by the scripts in
`scripts/data_construction/`. Its contents can be reproduced from the raw and
curated inputs and are therefore not versioned in this repository.

Run the construction scripts in numerical order (`01` through `04`) to rebuild
the complete set of processed datasets.
